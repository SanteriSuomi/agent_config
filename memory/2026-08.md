# Memory — 2026-08

## 2026-08-27: OOM without mem_limit
Load-time OOM on llama-server went global and froze the node ~40 min (OOM-kill restart storms). Fix: mem_limit/memswap 110g + oom_score_adj 800 — contained container kill, llama-server always the victim, never zomboid/plex. NEVER remove these.

## 2026-08-28: Qwen3.8-Flash-Next deployment
Replaced Ling-3.0-flash. Key discovery: `-lm mmap` REQUIRED on HIP — default auto anon-copies the whole model (RssAnon ~105 GB); mmap keeps weights file-backed. Daily-image rule: stock images ≥b10680 (b10666 had Vulkan graph-optimizer bug PR #27812 → silent wrong tokens).

## 2026-08-29: Vulkan gotchas + tuning
- `--flash-attn on` HALVES decode on Vulkan (use auto); `ngram-map-k` beats `ngram-mod`; `-b 4096 -ub 2048` = +25% prefill
- Decode-at-depth upstream-broken (#27856): ~14-16 t/s @100K ceiling
- PLE table ~26-28 GB in all 4-bit quants — quant choice doesn't fix page-cache thrash
- ROCm full-GPU retested on official b10666: still zero-syscall spin — dead until upstream changes
- Multi-slot works post PR #27742; `--chat-template-kwargs reasoning_effort` works; thinking-off per-request is 3.7× faster wall-clock

## 2026-08-30: Model bake-off → ROCmFP4-FAST wins
5 arms tested identically: stock+Q4_K_XL 19.9/15/9.5 t/s · fork+Q4_K_XL ngram · fork+Q4_K_XL+MTP · fork+IQ4_XS+MTP 22.3/21.6/19.6 · **fork+ROCmFP4-FAST+MTP 30.5/28.8/23.0** ← production. Lessons: MTP needs np1, worth +80-100% decode; word-salad benches hide drafter value — always bench code-copy; 4096/2048 batches trip the 110g breaker on ROCmFP4. CIRU + Ling + IQ4_XS fallbacks deleted.

## 2026-08-30 evening: fork 9a6f17e — depth curve solved
Pooled-key QSA cache + MTP hidden-state capture fix: decode @30K 23.0 → 30.0 t/s (flat curve). `--spec-draft-adaptive` loses overall — keep `n-max 3 p-min 0.75`.

## 2026-08-31: silent llama-server restart
One restart, no logged error, oom=false — suspect transient segfault in day-old fork pooled-cache path. If it recurs: `docker inspect llama-server --format '{{.State.ExitCode}}'` right after (137=OOM, 139=segfault). Disk cleaned same day (~66 GB reclaimed; only fork image + ROCmFP4 package kept).
