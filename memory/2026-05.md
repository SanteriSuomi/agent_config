# Memory — 2026-05

## 2026-05-17: Suggestarr schedule
Changed daily → weekly (Sundays 00:00), 5 movies + 2 shows per run, uses local llama-server.

## 2026-05-30: Reverse proxy + local DNS migration
- Replaced direct ip:port access with Caddy (Host-header routing on proxy_network) + CoreDNS (home.arpa zone, RFC 8375)
- Renamed jellyseerr → seerr; removed host port mappings from 18 services (kept Plex 32400, gluetun ports)
- systemd-resolved stub listener disabled, /etc/resolv.conf → 127.0.0.1 (immutable), dnsmasq disabled (port 53 conflict)
